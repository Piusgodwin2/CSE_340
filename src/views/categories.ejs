<%- include('partials/header')%>
    <main>
        <h1><%= title %></h1>
        <p>Here are a few of our categories:</p>
        <ul>
            <% categories.forEach(category => { %>
            <li>
                <%= category.category_name %>
            </li>
            <% }) %>
        </ul>
    </main>
<%- include('partials/footer')%>